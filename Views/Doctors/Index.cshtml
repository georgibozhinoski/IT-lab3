@model IEnumerable<lab3._1.Models.Doctor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>


<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HospitalId)
        </th>
        <th>
            @Html.DisplayName("Hospital Name")
        </th>
        <th>
            @Html.DisplayName("Hospital Address")
        </th>
        <th></th>
    </tr>
   
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HospitalId)
            </td>
            <td>
                <label> @Html.Action("GetHospitalName", "Doctors", new {id=item.HospitalId})</label>
            </td>
            
            <td> @Html.Action("GetHospitalAddress", "Doctors", new { id = item.HospitalId })</td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
